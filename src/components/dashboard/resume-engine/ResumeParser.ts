// Shared loose types for resume-engine sub-components.
// Intentionally permissive — consumers normalize the shape at runtime.
/* eslint-disable @typescript-eslint/no-explicit-any */
export type ParsedResume = any;
export type ParsedExperienceEntry = any;
